name: t_html_parser
description: "html package ကိုပဲ utils class ထပ်ထည့်ထားခြင်း။"
version: 1.0.0
homepage: https://github.com/ThanCoder/t_html_parser
repository: https://github.com/ThanCoder/t_html_parser

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  html: ^0.15.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
